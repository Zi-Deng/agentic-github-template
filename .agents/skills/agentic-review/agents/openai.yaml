interface:
  display_name: "Review Pull Request"
  short_description: "Run a fresh isolated independent review and publish findings"
  default_prompt: "Use $agentic-review to run a fresh isolated independent review and publish findings."
policy:
  allow_implicit_invocation: true
