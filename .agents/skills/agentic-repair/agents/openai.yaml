interface:
  display_name: "Repair Reviewed Pull Request"
  short_description: "Resume the original implementer session for review fixes"
  default_prompt: "Use $agentic-repair to resume the original implementer session for review fixes."
policy:
  allow_implicit_invocation: true
