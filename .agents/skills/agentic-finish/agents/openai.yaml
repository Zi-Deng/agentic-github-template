interface:
  display_name: "Prepare Human Merge"
  short_description: "Prepare verified merge, archival and cleanup"
  default_prompt: "Use $agentic-finish to prepare verified merge, archival and cleanup."
policy:
  allow_implicit_invocation: true
