interface:
  display_name: "Prepare Issue Worktree"
  short_description: "Create and register an isolated issue worktree"
  default_prompt: "Use $agentic-prepare to create and register an isolated issue worktree."
policy:
  allow_implicit_invocation: true
