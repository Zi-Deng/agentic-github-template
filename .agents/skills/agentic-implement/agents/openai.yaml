interface:
  display_name: "Implement Approved Issue"
  short_description: "Run managed implementation, checks and draft PR"
  default_prompt: "Use $agentic-implement to run managed implementation, checks and draft PR."
policy:
  allow_implicit_invocation: true
