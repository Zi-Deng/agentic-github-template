name: agentic workflow tests
on:
  pull_request:
  push:
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: agentic-quality-${{ github.ref }}
  cancel-in-progress: true
jobs:
  agentic-quality:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'
      - run: python -B scripts/agentic/check.py
      - name: Verify repository cleanliness
        run: |
          git diff --check
          python -c 'import subprocess; s=subprocess.check_output(["git","status","--porcelain"], text=True); print(s, end=""); raise SystemExit(bool(s))'
